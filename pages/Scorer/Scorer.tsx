import React from 'react';
import {Button, Text, Vibration, View,} from 'react-native';
import Timer from "../../components/Timer";

function Scorer(): React.JSX.Element {

    return (
        <View>
            <Timer></Timer>
        </View>
    );
}

export default Scorer;